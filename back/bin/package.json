{
  "name": "back",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
  "start": "node src/index.js",
  "build": "powershell -NoProfile -Command \"New-Item -ItemType Directory -Force bin | Out-Null; Copy-Item -Recurse -Force src bin; Copy-Item -Force package.json bin\"",
  "test": "node -e \"console.log('ok')\""
},
  "dependencies": {
    "express": "^4.19.0"
  }
}
